<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hys.trazar.mapper.RequestMapper">
	
	
	<insert id="addRequest" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
		INSERT INTO Request (title, body, price, memberId)
		VALUES (#{title}, #{body}, #{price}, #{memberId})
	</insert>
	
	<select id="listRequest" resultType="com.hys.trazar.domain.RequestDto">
		SELECT r.id, r.memberId, r.title, r.price, r.inserted
		FROM Request r 
		JOIN Member m ON m.id = r.memberId
		ORDER BY r.id DESC
	</select>
	
	<select id="getRequestById" resultType="com.hys.trazar.domain.RequestDto">
		SELECT r.id, r.memberId, r.title, r.body, r.price, r.inserted, m.phoneNum
		FROM Request r JOIN Member m ON r.memberId = m.id
		WHERE r.id = #{id}
	</select>
	
	<update id="updateRequest">
		UPDATE Request
	  	SET title = #{title}, body = #{body}, price = #{price}
	  	WHERE id = #{id}
	</update>
	
	<delete id="removeRequestById">
		DELETE FROM Request
		WHERE id = #{id}
	</delete>
	
	<delete id="deleteByMemberId">
		DELETE FROM Request
		WHERE memberId = #{memberId}
	</delete>
	
</mapper>